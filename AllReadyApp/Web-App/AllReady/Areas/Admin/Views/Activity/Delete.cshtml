@model AllReady.Models.Activity

@{  ViewData["Title"] = "Delete activity " + Model.Name; }

<div class="row">
    <div class="col-md-12">
        <h3>Are you sure you want to delete this?</h3>
        <h4>@ViewData["Title"]</h4>

    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.EndDateTimeUtc)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.EndDateTimeUtc)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.StartDateTimeUtc)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.StartDateTimeUtc)
            </dd>
        </dl>

        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <button type="submit" class="btn">Delete</button>
                <a class="btn" asp-controller="Campaign" asp-action="Details" asp-route-area="Admin" asp-route-id="@Model.CampaignId">Back to Campaign</a>
            </div>
        }
    </div>
</div>
